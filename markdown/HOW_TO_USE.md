# 如何使用现有的AI交易系统 🎯

## ✅ 现有功能说明

你的系统**已经完整集成**了AI交易控制功能！不需要额外的组件。

## 🏃 使用步骤

### 1. 启动后端

```bash
cd /data/proj/open_nof1/nof0/backend
npm start
```

后端将在 http://localhost:3001 启动

### 2. 启动前端

```bash
cd /data/proj/open_nof1/nof0/web
npm run dev
```

前端将在 http://localhost:3000 启动

### 3. 使用前端控制

打开浏览器访问 http://localhost:3000，你会看到：

```
┌────────────────────────────────────────┐
│ Prompt Studio Chat 面板                │  ← 左侧面板
├────────────────────────────────────────┤
│ ┌─ 交易控制 ──────────────────────┐   │
│ │ 环境: [demo-futures ▼]          │   │
│ │ AI预设: [(默认) ▼]              │   │  
│ │ 间隔(分): [3]                   │   │
│ │ [启动] [停止] 状态: 未运行      │   │
│ └──────────────────────────────────┘   │
│                                        │
│ [问答] [建议] [显示] ...               │
└────────────────────────────────────────┘
```

## 📡 前端已有的功能

### 交易控制区（在PromptStudioChatPanel中）

**配置项：**
- 🌍 **环境选择**：demo-futures, demo-spot, futures, spot
- 🤖 **AI预设**：从配置文件读取的AI模型预设
- ⏱️ **运行间隔**：设置AI交易决策的频率（分钟）

**操作按钮：**
- 🚀 **启动**：开始定时运行AI交易系统
- 🛑 **停止**：停止交易系统
- 📊 **状态显示**：实时显示运行状态

### Prompt Studio功能

- **问答**：与AI对话优化交易提示
- **建议**：让AI提供交易提示模板建议
- **显示**：查看当前使用的提示模板
- **Diff**：对比新旧提示模板差异
- **应用**：应用新的提示模板
- **保存**：保存当前配置
- **回滚**：恢复到之前的模板
- **占位符**：查看可用的模板占位符
- **能力**：查看系统支持的功能

## 🔧 后端API端点（已存在）

前端已经在调用这些端点：

```
GET  /api/nof1/ai/trading/status  - 获取运行状态
POST /api/nof1/ai/trading/start   - 启动交易系统
POST /api/nof1/ai/trading/stop    - 停止交易系统
GET  /api/nof1/conversations      - 获取交易对话
```

## 📂 数据文件位置

交易系统运行后会生成：

```
backend/data/
├── conversations.json    # AI对话记录
├── trading-state.json    # 交易状态
└── trades.json          # 交易记录
```

## 🐛 常见问题

### Q1: 点击"启动"没反应？

**检查：**
```bash
# 1. 确认后端正常运行
curl http://localhost:3001/api/health

# 2. 检查浏览器控制台是否有错误
# 打开浏览器开发者工具（F12）查看Console

# 3. 检查后端日志
# 查看运行npm start的终端输出
```

### Q2: 如何查看AI生成的数据？

```bash
# 查看最新对话
cat backend/data/conversations.json | jq '.conversations[0]'

# 查看交易状态
cat backend/data/trading-state.json

# 查看所有交易
cat backend/data/trades.json
```

### Q3: 如何修改AI配置？

编辑配置文件：
```bash
# AI交易配置
backend/ai/ai-trading/config.json

# 环境变量
backend/.env
```

### Q4: 系统路径在哪里？

现有系统使用的是：
```
backend/ai/ai-trading/
├── run-ai-trading.mjs         # 定时运行脚本
├── ai-trading-system.v2.mjs   # AI交易系统主文件  
├── config.json                # 配置文件
└── prompt_templates/          # 提示模板目录
```

## ⚠️ 重要说明

**不要重复创建**以下内容（因为已经存在）：
- ❌ 新的交易控制组件（已集成在PromptStudioChatPanel）
- ❌ 新的API端点（已存在于nof1.js）
- ❌ 新的tradingRunner服务（已有runner.js）

**你只需要**：
1. ✅ 启动后端
2. ✅ 启动前端
3. ✅ 在前端点击"启动"按钮

## 📊 完整工作流程

```
前端（Prompt Studio）
  点击"启动"按钮
      ↓
  POST /api/nof1/ai/trading/start
      ↓
后端（nof1.js router）
      ↓
tradingRunner.start()
      ↓
spawn node run-ai-trading.mjs
      ↓
定时执行 ai-trading-system.v2.mjs
      ↓
保存数据到 backend/data/
      ↓
前端自动刷新显示最新数据
```

## 🎉 现在就试试！

1. 打开终端1：`cd backend && npm start`
2. 打开终端2：`cd web && npm run dev`
3. 打开浏览器：http://localhost:3000
4. 点击左侧面板的"启动"按钮
5. 等待几分钟，刷新页面查看交易对话记录

就这么简单！🚀

