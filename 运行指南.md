# NOF0 项目运行指南

## 📋 项目结构

```
nof0/
├── backend/     # 后端服务 (Node.js + Express)
├── web/         # 前端服务 (Next.js)
└── ...
```

## 🚀 快速启动

### 方式一：分别启动（推荐用于开发）

#### 1. 启动后端服务

```bash
# 进入后端目录
cd /data/proj/open_nof1/nof0/backend

# 检查依赖（如果 node_modules 不存在，需要先安装）
# npm install

# 启动后端服务（开发模式，支持热重载）
npm run dev

# 或者生产模式
npm start
```

**后端服务运行在**: `http://localhost:3001`

#### 2. 启动前端服务

```bash
# 打开新的终端窗口，进入前端目录
cd /data/proj/open_nof1/nof0/web

# 检查依赖（如果 node_modules 不存在，需要先安装）
# npm install

# 启动前端服务
npm run dev
```

**前端服务运行在**: `http://localhost:3000`

---

### 方式二：后台运行（使用 nohup 或 screen）

#### 启动后端（后台运行）

```bash
cd /data/proj/open_nof1/nof0/backend
nohup npm run dev > ../logs/backend.log 2>&1 &
```

#### 启动前端（后台运行）

```bash
cd /data/proj/open_nof1/nof0/web
nohup npm run dev > ../logs/frontend.log 2>&1 &
```

#### 查看日志

```bash
# 查看后端日志
tail -f /data/proj/open_nof1/nof0/logs/backend.log

# 查看前端日志
tail -f /data/proj/open_nof1/nof0/logs/frontend.log
```

---

### 方式三：使用 screen（推荐用于SSH连接）

#### 启动后端

```bash
# 创建新的 screen 会话
screen -S nof0-backend

# 进入后端目录并启动
cd /data/proj/open_nof1/nof0/backend
npm run dev

# 按 Ctrl+A 然后按 D 来 detach（保持运行）
```

#### 启动前端

```bash
# 创建新的 screen 会话
screen -S nof0-frontend

# 进入前端目录并启动
cd /data/proj/open_nof1/nof0/web
npm run dev

# 按 Ctrl+A 然后按 D 来 detach（保持运行）
```

#### 重新连接 screen 会话

```bash
# 查看所有 screen 会话
screen -ls

# 重新连接后端
screen -r nof0-backend

# 重新连接前端
screen -r nof0-frontend
```

---

## 🔍 检查服务状态

### 检查端口占用

```bash
# 检查端口 3000 (前端)
lsof -i :3000

# 检查端口 3001 (后端)
lsof -i :3001

# 或者使用 ss 命令
ss -tlnp | grep -E ":(3000|3001)"
```

### 测试服务是否正常

```bash
# 测试后端健康检查
curl http://localhost:3001/api/nof1/health

# 测试前端
curl http://localhost:3000
```

---

## 🛑 停止服务

### 停止后端

```bash
# 查找后端进程
ps aux | grep "node.*server.js" | grep -v grep

# 停止进程（替换 PID 为实际进程ID）
kill <PID>

# 或者强制停止
kill -9 <PID>
```

### 停止前端

```bash
# 查找前端进程
ps aux | grep "next dev" | grep -v grep

# 停止进程
kill <PID>
```

### 批量停止

```bash
# 停止所有 node 进程（谨慎使用）
pkill -f "node.*server.js"
pkill -f "next dev"
```

---

## 📝 环境变量配置

### 后端环境变量

后端环境变量文件：`backend/.env`

```bash
# 查看环境变量
cat backend/.env

# 主要配置项：
# - BINANCE_DEMO_API_KEY
# - BINANCE_DEMO_API_SECRET
# - PORT (默认: 3001)
```

### 前端环境变量

前端环境变量文件：`web/.env.local`（如果存在）

```bash
# 主要配置项：
# - NEXT_PUBLIC_URL (默认: http://localhost:3000)
```

---

## 🔧 常见问题

### 1. 端口被占用

```bash
# 查看占用端口的进程
lsof -i :3000
lsof -i :3001

# 停止占用端口的进程
kill -9 <PID>
```

### 2. 依赖未安装

```bash
# 后端依赖
cd backend
npm install

# 前端依赖
cd web
npm install
```

### 3. 服务启动失败

```bash
# 检查 Node.js 版本（建议 v18+）
node --version

# 查看详细错误信息
npm run dev 2>&1 | tee error.log
```

---

## 📊 访问地址

启动成功后，访问：

- **前端界面**: http://localhost:3000
- **后端API**: http://localhost:3001/api/nof1
- **健康检查**: http://localhost:3001/api/nof1/health

---

## 💡 推荐工作流程

### 开发环境

1. **终端1**: 启动后端
   ```bash
   cd /data/proj/open_nof1/nof0/backend
   npm run dev
   ```

2. **终端2**: 启动前端
   ```bash
   cd /data/proj/open_nof1/nof0/web
   npm run dev
   ```

3. **浏览器**: 访问 http://localhost:3000

### 生产环境（后台运行）

使用 screen 或 nohup 在后台运行服务，确保 SSH 断开后服务继续运行。
